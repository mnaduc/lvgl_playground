{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug LVGL Playground",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/lvgl_playground",
            "cwd": "${workspaceFolder}",
            "MIMode": "gdb",
            "preLaunchTask": "CMake: build"
        }
    ]
}
